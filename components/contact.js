window.PORTFOLIO_VIEWS.contact = (site) => {
  const { esc, icon, gmail, safeURL, image } = window.P;
  return `
    <div class="contact-layout">
      <div class="contact-intro"><div class="page-heading reveal"><p class="eyebrow"><span class="tiny-line"></span> THE NEXT CHAPTER</p><h1 tabindex="-1">Have a spark?<br>Let's make<br><em>it move.</em> <span class="contact-star" aria-hidden="true">${icon('star')}</span></h1><p>A game to launch. A story to tell.<br>Or an idea that's just getting started.</p></div><div class="contact-person reveal"><img src="${esc(image(site.avatar))}" alt="" width="48" height="48"><div><strong>One real person. Plenty of ideas.</strong><span><i class="status-dot ${site.available ? '' : 'unavailable'}"></i>${esc(site.available ? site.availability : 'Focused on current projects')}</span></div></div><div class="social-list reveal">${site.socials.filter((s) => safeURL(s.url)).map((social) => `<a href="${esc(safeURL(social.url))}" target="_blank" rel="noopener noreferrer" class="social-link">${icon(social.icon)}<span>${esc(social.name)}</span>${icon('arrow-up')}</a>`).join('')}</div></div>
      <div class="contact-card reveal"><div class="contact-card-top"><span class="contact-envelope">${icon('mail')}</span><span class="eyebrow">GOOD THINGS START WITH HELLO</span></div><div class="email-row"><a id="email-link" href="${esc(gmail())}" target="_blank" rel="noopener noreferrer">${esc(site.email)}</a><button type="button" class="icon-button" data-action="copy-email" aria-label="Copy email address">${icon('copy')}</button></div><div class="contact-divider"></div><form id="contact-form"><label class="form-label" for="project-type">What are we creating?</label><div class="select-wrap"><select id="project-type" name="project-type"><option value="Game trailer">A game trailer</option><option value="Game ad">A creative game ad</option><option value="Social content">Social content</option><option value="Something else">Something a little different</option></select></div><label class="form-label" for="project-brief">A little about your idea <span>(optional)</span></label><textarea id="project-brief" name="project-brief" rows="3" maxlength="2000" placeholder="The game, the feeling, the big idea..."></textarea><button type="submit" class="button button-primary contact-submit">Start a conversation ${icon('arrow-up')}</button><p class="form-note">Opens a Gmail draft. Nothing is sent automatically.</p><p id="compose-fallback" class="compose-fallback" hidden>Your browser blocked the new window. <a href="${esc(gmail())}" target="_blank" rel="noopener noreferrer">Open your email draft here.</a></p></form><div class="contact-card-footer"><span>${icon('pin')} ${esc(site.location)}</span><a href="mailto:${esc(site.email)}">Use another email app ${icon('arrow-up')}</a></div></div>
    </div>`;
};
